import { HightlightDirective } from './hightligh.directive';

describe('HightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightDirective();
    expect(directive).toBeTruthy();
  });
});
